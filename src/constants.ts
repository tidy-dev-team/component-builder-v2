export const PROPERTY_PREFIXES = {
  TEXT_DEPENDENCY: "✏️",
  INSTANCE_SWAP_DEPENDENCY: "🔁",
} as const;

export const HIDDEN_PROPERTIES = ["type", "state"] as const;

export const UI_DIMENSIONS = {
  HEIGHT: 500,
  WIDTH: 320,
} as const;