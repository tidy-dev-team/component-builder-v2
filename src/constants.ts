export const PROPERTY_PREFIXES = {
  TEXT_DEPENDENCY: "✏️",
  INSTANCE_SWAP_DEPENDENCY: "🔁",
} as const;

export const HIDDEN_PROPERTIES = ["selection type"] as const;

export const UI_DIMENSIONS = {
  HEIGHT: 800,
  WIDTH: 800,
} as const;
