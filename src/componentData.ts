// Re-export from the new component_data directory
// Note: Since we use generic generators, these are just for initial component keys
// Any component can be added dynamically using getComponentData()
export { componentRegistry as components } from "./component_data";
